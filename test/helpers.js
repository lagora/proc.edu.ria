import 'babel-polyfill';
import * as helpers from '../src/helpers';
import assert from 'assert';

describe('helpers.*', () => {
  xit('helpers.scan', () => {});
  xit('helpers.md5', () => {});
  xit('helpers.hashPadding', () => {});
  xit('helpers.add', () => {});
  xit('helpers.to', () => {});
});
